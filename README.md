# ssf-inventory

## How to Clone the Repository

Follow these steps to download the project to your local computer.

### 1. Install Git

Make sure Git is installed on your computer.

You can download Git here:

https://git-scm.com/downloads

After installing Git, open **Command Prompt**, **PowerShell**, or **Git Bash**.

Check if Git is installed:

```bash
git --version
